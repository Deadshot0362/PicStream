# .github/workflows/deploy.yml

name: Deploy Jekyll site to GitHub Pages

on:
  push:
    branches:
      - main  # Change if you're using a different branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '2.7'  # Specify Ruby version 2.7

      - name: Install dependencies
        run: |
          gem install bundler -v 2.4.22  # Install bundler version 2.4.22 for Ruby 2.7
          bundle install

      - name: Build Jekyll site
        run: |
          bundle exec jekyll build --source src --destination dist

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
          branch: gh-pages  # Adjust to the correct branch if necessary
